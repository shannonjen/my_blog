source 'http://rubygems.org'

gem 'sinatra'
# extends sinatra with extension methods and rake tasks
# for dealing with a SQL database using activerecord
gem 'sinatra-activerecord'

# SQL database
gem 'sqlite3'

gem 'rake'

# provides automatic application reloading 
# restarts the server on every request 
gem 'shotgun'


